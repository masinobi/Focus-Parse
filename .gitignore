# dependencies
node_modules

# build output
.next
out
*.tsbuildinfo
next-env.d.ts

# generated by the postinstall script from node_modules/pdfjs-dist
public/pdf.worker.min.mjs

# secrets — never commit; see .env.example
.env
.env*.local

# editor / os
.DS_Store
Thumbs.db
.idea
.vscode

# Scratch beside the scanners: probe screenshots, measurement scripts, the
# throwaway edit scripts, the backups taken before deliberately breaking
# something. Every dot-prefixed file in here, because listing the extensions
# one at a time has now let .py and .bak through twice.
scripts/.*
